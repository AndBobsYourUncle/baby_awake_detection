# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
PyYAML>=6.0

# ViTPose dependencies (default backend)
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# Person detection (filters out frames with no person)
ultralytics>=8.0.0

# MediaPipe (optional - only if using --backend mediapipe)
# mediapipe>=0.10.0
